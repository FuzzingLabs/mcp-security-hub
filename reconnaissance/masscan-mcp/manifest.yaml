name: masscan-mcp
category: reconnaissance
type: full
tools:
- name: masscan_scan
  required_params:
  - targets
  - ports
- name: masscan_top_ports
  required_params:
  - targets
- name: get_scan_results
  required_params:
  - scan_id
- name: list_active_scans
