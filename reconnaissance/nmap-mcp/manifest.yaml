name: nmap-mcp
category: reconnaissance
type: full
tools:
- name: port_scan
  required_params:
  - target
- name: service_scan
  required_params:
  - target
- name: os_detection
  required_params:
  - target
- name: script_scan
  required_params:
  - target
- name: quick_scan
  required_params:
  - target
- name: get_scan_results
  required_params:
  - scan_id
- name: list_active_scans
