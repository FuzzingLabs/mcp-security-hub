name: nuclei-mcp
category: web-security
type: full
tools:
- name: nuclei_scan
  required_params:
  - target
- name: quick_scan
  required_params:
  - target
- name: template_scan
  required_params:
  - target
  - templates
- name: list_templates
- name: get_scan_results
  required_params:
  - scan_id
- name: list_active_scans
